# vim-day14
## 今日目标
> 掌握悬浮显式 & 大小写 & 注释

## 操作
### 1. 悬浮显示
+ `gh`，悬浮显示

### 2. 大小写
+ 在normal模式下，
  + `gu`，小写
  + `gU`，大写
+ 在`visual模式`下，
  + `u`，小写
  + `U`，大写
+ `~`，大小写互换

### 3. 注释
> normal模式与visual模式通用
+ `gc`，单行注释
+ `gC`，多行注释
